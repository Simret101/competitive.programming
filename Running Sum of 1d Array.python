class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        tot=0
        sumArray=[]
        for i in nums:
            tot+=i
            sumArray.append(tot)
        return sumArray

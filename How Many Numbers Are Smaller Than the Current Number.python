class Solution:
    def smallerNumbersThanCurrent(self, nums: List[int]) -> List[int]:
        sort=sorted(nums)
        list=[]
        for i in range (len(nums)):
            x=sort.index(nums[i])
            list.append(x)
        return list

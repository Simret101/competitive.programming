#include <iostream>
#include<math.h>
using namespace std;

int main()
{
    double n,m,a;
    cin>>n>>m>>a;
    long long first_line_flagstones=ceil(n/a);
    long long iteration_length=ceil(m/a);
    long long result=first_line_flagstones*iteration_length;
    cout<<result<<endl;
}
